import { Image, StyleSheet, Text, View } from "react-native";
import X from './X';
import O from './O';

export default function Placar({ x, o }) {
    return <View style={styles.jogo}>

    </View>
}
const styles = StyleSheet.create({
    jogo: {
        width: "80%",
        aspectRatio: 1,
        backgroundColor: "#fff",
        borderRadius: 20
    },
})